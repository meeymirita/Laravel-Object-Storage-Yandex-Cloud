import { initFileUpload } from './file-upload';
import { initFileList } from './get-files.js';

document.addEventListener('DOMContentLoaded', initFileUpload);
document.addEventListener('DOMContentLoaded', initFileList);
